/**
 * Helper function to create a Page object.
 */
export function createPage(index, width, height, textElements) {
    return {
        index,
        width,
        height,
        textElements,
    };
}
